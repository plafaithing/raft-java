#!/bin/bash

# 高延迟网络测试指南（代码层面）

echo "=========================================="
echo "  高延迟网络测试指南（代码层面）"
echo "=========================================="
echo ""

echo "由于 WSL 环境限制，tc 命令可能不可用。"
echo "可以使用以下方法模拟高延迟网络："
echo ""

echo "方法 1：在 RPC 调用中添加人工延迟"
echo "----------------------------------------"
echo "修改 Peer.java，在 sendAppendEntries() 方法中添加延迟："
echo ""
echo "  public void sendAppendEntries() {"
echo "      // 模拟网络延迟"
echo "      try {"
echo "          Thread.sleep(100);  // 100ms 延迟"
echo "      } catch (InterruptedException e) {"
echo "          Thread.currentThread().interrupt();"
echo "      }"
echo "      // 原有的 RPC 调用逻辑"
echo "      raftService.appendEntries(request, callback);"
echo "  }"
echo ""

echo "方法 2：修改 RaftOptions.java"
echo "----------------------------------------"
echo "添加网络延迟配置："
echo ""
echo "  // 模拟网络延迟（毫秒）"
echo "  private long simulatedNetworkDelay = 100;"
echo ""
echo "然后在 Peer.java 中使用："
echo "  Thread.sleep(raftOptions.getSimulatedNetworkDelay());"
echo ""

echo "方法 3：使用 Docker 网络延迟"
echo "----------------------------------------"
echo "如果使用 Docker 运行 Raft 集群："
echo ""
echo "  docker run --network-delay=100ms ..."
echo ""

echo "=========================================="
echo "  测试步骤"
echo "=========================================="
echo ""
echo "1. 修改代码，添加网络延迟模拟"
echo "2. 重新编译: mvn clean package -DskipTests"
echo "3. 测试基准值（禁用批量优化）"
echo "4. 测试优化值（启用批量优化）"
echo "5. 对比结果"
echo ""
echo "预期结果："
echo "  - 高延迟网络下，批量优化应该能显著提升吞吐量"
echo "  - 网络效率应该有明显提升"
